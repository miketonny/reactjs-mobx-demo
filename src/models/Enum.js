//enum for status
export const ColumnStatus = Object.freeze({
    Processing: 'processing',
    Started: 'started',
    Stopped: 'stopped',
    Idle: 'idle'
});

export const ColumnType = Object.freeze({
    Trim: 'trim',
    Split:'split',
    Other: 'other'
});